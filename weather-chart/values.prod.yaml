image:
  tag: latest

replicaCount: 3

hpa:
  minReplicas: 3
  maxReplicas: 20
  cpuUtilization: 50
  memoryUtilization: 70

config:
  defaultCity: Seoul

ingress:
  host: weather.local

resources:
  requests:
    cpu: "200m"
    memory: "256Mi"
  limits:
    cpu: "1000m"
    memory: "512Mi"